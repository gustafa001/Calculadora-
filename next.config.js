/** @type {import('next').NextConfig} */
const nextConfig = {
  // Substitua pelo seu Publisher ID do AdSense quando aprovado
  // async headers() {
  //   return [{ source: '/(.*)', headers: [{ key: 'X-Frame-Options', value: 'DENY' }] }]
  // },
}

module.exports = nextConfig
